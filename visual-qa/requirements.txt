# Visual QA Python dependencies
#
# Core (required):
#   pytest — test runner for tests/test_compare.py
#   PyYAML — actions.yaml parsing in navigator.py
#
# Optional:
#   Pillow — enables screenshot comparison via --ssim flag in compare.py

pytest>=7.0
Pillow==12.1.1
PyYAML==6.0.2
